name: Validate ticket naming

on:
  pull_request:
    types: [opened, edited, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  validate:
    uses: derliebemarcus/maintenance/.github/workflows/validate-ticket-naming.yml@main
