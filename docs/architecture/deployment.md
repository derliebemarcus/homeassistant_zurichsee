# Deployment architecture

Zürichsee Wetterstationen Home Assistant Integration is deployed as Home Assistant custom integration runtime. Deployment and release automation validate the repository before mutating runtime state. See the [deployment diagram](diagrams/deployment.md).
