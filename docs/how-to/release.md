# Release

1. Record release intent using the repository's established release mechanism.
2. Merge only after required Jenkins and GitHub checks are successful.
3. Create immutable version tags and release assets through automation.
4. Document breaking changes and migration steps before publication.

Files below `.changeset/`, release metadata, manifests, and lockfiles are not documentation-only changes.
