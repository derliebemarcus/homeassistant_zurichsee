# Local development

1. Create a Python environment using the versions and constraints in the repository.
2. Run the Jenkins task wrappers for tests, linting, typing, translations, and audits.
3. Validate both unit tests and Home Assistant integration tests.

Keep repository-specific secrets outside Git and preserve generated-file boundaries.
