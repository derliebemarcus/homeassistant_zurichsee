# Concepts

Home Assistant custom integration for weather-station data around Lake Zurich provided through the tecdottir API.

The repository executes as Home Assistant custom integration runtime and uses Python, Home Assistant, tecdottir HTTP API, HACS.
