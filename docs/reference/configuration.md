# Configuration

- The config flow selects one or more weather stations.
- The update interval is configurable in supported increments.
- Entity availability depends on the measurements exposed by each station.

Secrets are referenced from protected runtime stores and must never be committed.
