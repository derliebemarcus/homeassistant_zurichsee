# Environment variables

Environment variables are deployment-specific unless explicitly documented by the project. Store credentials and secret values in protected host or CI credential stores. Keep examples non-sensitive and fail closed when a required value is absent.
